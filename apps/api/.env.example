# Postgres connection string used by @wholesale/db
DATABASE_URL="postgresql://user:password@localhost:5432/wholesale_store"

# Secret used to sign admin session tokens. Generate one with:
#   openssl rand -base64 32
JWT_SECRET="replace-with-a-long-random-string"

# Set to "production" when deployed so session cookies are marked Secure
NODE_ENV="development"
